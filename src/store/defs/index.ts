import { IndexState } from "./types";

import user from "./user";

export default {
    state: (): IndexState => ({ }),

    getters: { },
    mutations: { },
    actions: { },
    modules: { user }
};
