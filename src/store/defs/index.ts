import { IndexState } from "./types";

import cards from "./cards";

export default {
    state: (): IndexState => ({ }),

    getters: { },
    mutations: { },
    actions: { },
    modules: { cards }
};
